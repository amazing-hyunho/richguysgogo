import type{Evaluation,GameStats}from'../types';
export function localEvaluation(text:string,first:boolean,waiting:boolean):Evaluation{const question=/[?？]|(어디|언제|무엇|뭐|어떻게|왜|몇|좋아하세요|하셨어요|인가요|세요)$/.test(text.trim());return{greeting:first&&/안녕|반갑|처음 뵙/.test(text),question,reaction:/진짜|그러셨|그렇군|우와|대단|재밌|아 그렇/.test(text),empathy:/힘드셨|불편하시|속상|좋으셨|기쁘셨|걱정|괜찮으세요/.test(text),conversationInitiative:question,silenceBreak:waiting&&question}}
export function applyEvaluation(s:GameStats,e:Evaluation){let n=0;if(e.greeting&&!s.greeting)n+=10;if(e.question)n+=10;if(e.reaction)n+=8;if(e.empathy)n+=15;if(e.silenceBreak)n+=15;return{...s,score:Math.min(100,s.score+n),greeting:s.greeting||e.greeting,questions:s.questions+(e.question?1:0),reactions:s.reactions+(e.reaction?1:0),empathy:s.empathy+(e.empathy?1:0),silenceBreaks:s.silenceBreaks+(e.silenceBreak?1:0)}}
export const earnedBadges=(s:GameStats,d:number)=>[['🏅 첫인사왕',s.greeting],['🏅 질문왕',s.questions>=3],['🏅 리액션왕',s.reactions>=3],['🏅 공감왕',s.empathy>0],['🏅 침묵파괴자',s.silenceBreaks>0],['🏅 대화마스터',s.elapsed>=d]].filter(([,ok])=>ok).map(([n])=>n as string);
